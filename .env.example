# --- n8n endpoints (adjust your public or tunnel URLs) ---
N8N_CHAT_URL=https://<tu-n8n>/webhook/chat
N8N_UPLOAD_URL=https://<tu-n8n>/webhook/upload
N8N_API_TOKEN=changeme-token  # el mismo que validarás en n8n

# Local mode (optional) if you want to query OpenAI directly from Streamlit
OPENAI_API_KEY=
